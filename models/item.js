const getDb = require('../util/db').getDb;

class Item {
    constructor(title, price, description, imagePath) {
        this.title = title;
        this.price = price;
        this.description;
        this.imagePath;
    }

    save() {
        
    }
}